image: node:latest

stages:
  - tests

test_async:
  stage: tests
  script:
   - npm install -g lerna
   - yarn clean
   - lerna bootstrap
   - yarn lint
   - yarn test
   - yarn test:e2e
  tags:
    - docker
