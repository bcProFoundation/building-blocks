apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: myaccount-castlecraft-in-prod
  namespace: default
spec:
  secretName: myaccount-castlecraft-in-prod-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: myaccount.castlecraft.in
  dnsNames:
  - myaccount.castlecraft.in
  acme:
    config:
    - http01:
        ingressClass: nginx
      domains:
      - myaccount.castlecraft.in
    - http01:
        ingress: infrastructure-console-prod
      domains:
      - myaccount.castlecraft.in
