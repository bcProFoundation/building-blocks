apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: myaccount-castlecraft-in-staging
  namespace: default
spec:
  secretName: myaccount-castlecraft-in-staging-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: staging-myaccount.castlecraft.in
  dnsNames:
  - staging-myaccount.castlecraft.in
  acme:
    config:
    - http01:
        ingressClass: nginx
      domains:
      - staging-myaccount.castlecraft.in
    - http01:
        ingress: infrastructure-console-staging
      domains:
      - staging-myaccount.castlecraft.in
