{
  "private": true,
  "scripts": {
    "docs": "./scripts/build-docs.sh",
    "version": "lerna clean --yes && lerna bootstrap --ignore-scripts -- --package-lock-only --no-audit && git add apps/*/package-lock.json frontends/*/package-lock.json"
  },
  "devDependencies": {
    "@types/node": "^12.6.9",
    "lerna": "^3.16.4",
    "npm-check": "^5.9.0",
    "typedoc": "^0.15.0"
  }
}
